# Azure-User-Story-Automation
This repository contains an Azure DevOps automation solution that listens to User Story creation events and automatically creates five predefined child Tasks. The solution ensures correct parent–child relationships, inherits Area and Iteration paths, prevents duplicate task creation (idempotent design)
